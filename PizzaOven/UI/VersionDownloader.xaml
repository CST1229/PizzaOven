<Window x:Class="PizzaOven.VersionDownloader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:local="clr-namespace:PizzaOven"
        mc:Ignorable="d" Background="#202020" Title="Version Downloader" WindowStartupLocation="CenterScreen"
        MinWidth="300" MinHeight="300"
        Width="800" Height="500"
>
    <Window.Resources>
        <DataTemplate x:Key="VersionTemplate">
            <Border CornerRadius="5" Margin="10 3 10 3">
                <Border.BorderBrush>
                    <SolidColorBrush Color="#202020" Opacity="0.75"/>
                </Border.BorderBrush>
                <Border.Background>
                    <SolidColorBrush Color="#202020" Opacity="0.75"/>
                </Border.Background>
                <Grid VerticalAlignment="Center" x:Name="GridItem" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35" />
                        <RowDefinition Height="30" />
                    </Grid.RowDefinitions>
                    <Rectangle Grid.RowSpan="2" Stretch="UniformToFill" VerticalAlignment="Center"/>

                    <TextBlock x:Name="Title" Grid.Column="0" Margin="5 0 5 0" Grid.Row="0" FontWeight="Medium" FontSize="24" TextTrimming="WordEllipsis" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Top"  Foreground="#f2f2f2" Text="{Binding DisplayName}"/>
                    <Button Content="Download" FontSize="18" Grid.Column="1" FontWeight="Medium" Padding="10,3" Margin="5,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Right" Background="#181818" BorderBrush="#252525" Foreground="#f2f2f2" Click="DownloadVersion_Click"/>
                    <Grid Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Grid.Row="1" Margin="5 0 5 0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="Date" Margin="5" FontWeight="Medium" FontSize="16" TextTrimming="WordEllipsis" Background="Transparent" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Foreground="#aaaaaa" Text="{Binding SteamDBDateString}"/>
                    </Grid>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Name="SearchBarGrid" Margin="5,5,10,5" Height="30" VerticalAlignment="Center">
            <TextBox FontSize="16" Height="30" Margin="0,0,30,0" Background="#353535" Foreground="#f2f2f2" BorderBrush="#353535" x:Name="SearchBar" KeyDown="SearchBar_KeyDown" Padding="3" TextWrapping="NoWrap"/>
            <TextBlock FontSize="16" Height="30" Padding="8,5,5,5" Opacity="0.5" Foreground="#f2f2f2" Panel.ZIndex="3" IsHitTestVisible="False" HorizontalAlignment="Left" Text="Search..." VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=SearchBar, Path=Text.IsEmpty}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <fa5:ImageAwesome Icon="Solid_Times" Foreground="#f2f2f2" HorizontalAlignment="Right" Margin="6,6,35,6" Height="10" PreviewMouseLeftButtonDown="Clear_PreviewMouseLeftButtonDown">
                <fa5:ImageAwesome.Style>
                    <Style TargetType="fa5:ImageAwesome">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=ModGrid_SearchBar, Path=Text.IsEmpty}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ElementName=ModGrid_SearchBar, Path=Text.IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </fa5:ImageAwesome.Style>
            </fa5:ImageAwesome>
            <Button HorizontalAlignment="Right" Panel.ZIndex="3" Focusable="False" Click="SearchButton_Click" Name="SearchButton" Height="30" Width="30">
                <fa5:ImageAwesome Icon="Solid_Search" Foreground="#f2f2f2" Margin="3"/>
            </Button>
        </Grid>
        <Border CornerRadius="5" Grid.Row="1" Margin="5,5,5,5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border.BorderBrush>
                <SolidColorBrush Color="Black" Opacity="0.75"/>
            </Border.BorderBrush>
            <Border.Background>
                <SolidColorBrush Color="Black" Opacity="0.75"/>
            </Border.Background>
            <ListView Margin="5" Panel.ZIndex="1" Style="{StaticResource ListviewStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                ScrollViewer.CanContentScroll="False" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="Transparent" BorderBrush="Transparent" Name="VersionGrid"
                ItemTemplate="{StaticResource VersionTemplate}" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Focusable" Value="false"/>
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Border>
        <TextBlock
            Grid.Row="2" FontSize="12" TextWrapping="Wrap" Opacity="0.5" FontStyle="Italic" Padding="3,0,3,0"
            Foreground="#f2f2f2" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
            Text="Note: The letters after version numbers like V1.0.311c are unofficial, and serve to distinguish updates with otherwise identical version numbers."
        />
        <TextBlock
            Grid.Row="3" FontSize="12" TextWrapping="Wrap" Opacity="0.5" FontStyle="Italic" TextAlignment="Center"
            Padding="3,0,3,5" Foreground="#f2f2f2" HorizontalAlignment="Center" VerticalAlignment="Center"
            Text="Usually, these identically-numbered versions only differ by credits or localization changes."
        />
    </Grid>
</Window>
